package ch06;

// 1. 클래스를 설계하는 측
public class Warrior {
    
    // 멤버 변수
    // 맴버 변수를 초기화 하지 않으면
    // 인스턴스화(메모리에 올리다) 될 때
    // 기본값으로 초기화 된다.

    // 속성(상태) 설계
    String name;
    int health;
    int power;
    int dp;
    String color;
}
